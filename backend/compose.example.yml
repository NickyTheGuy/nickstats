services:
  nickstats-api:
    build: .
    restart: unless-stopped
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - "8000"
    networks:
      - nickstats

networks:
  nickstats:
    name: nickstats

